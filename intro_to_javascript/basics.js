// Variables
// Variables are simply containers that store values
// They follow this patternL
var yourVariable = yourValue;

// They can store of the data types we have seen
var name = "Rusty";
var secretNumber = 73;
var isAdorable = true;

// Recall the stored value by calling the variable name
var name = "Rusty";
"hello there " + name   // "hello there Rusty"

var num = 36;
num + 3 + 10    // 49

// We can also update existing variables
var name = "Robert";
name = "Bob";